import type { App } from 'vue';
import CustomCard from './custom-card';
import CustomForm from './custom-form';
import CustomList from './custom-list';

export default { CustomCard, CustomForm, CustomList };
